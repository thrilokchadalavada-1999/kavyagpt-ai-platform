apiVersion: v1
kind: Service

metadata:
  name: backend-service
  namespace: kavyagpt

spec:
  selector:
    app: backend

  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000

  type: NodePort